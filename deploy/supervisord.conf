[supervisord]
nodaemon=true
logfile=/tmp/supervisord.log
pidfile=/tmp/supervisord.pid
user=root

# ════════════════════════════════════════════════════════════════════════════════
# 👑 AUREON TRADING SYSTEM - SUPERVISOR CONFIGURATION
# ════════════════════════════════════════════════════════════════════════════════
#
# STARTUP ORDER (Critical for proper wiring):
#   1. Command Center (UI + loads all scanner/brain systems internally)
#   2. Orca Kill Cycle (Waits for Queen to be ready, then executes)
#   3. Autonomous Engine (9 parallel intelligence loops)
#
# ARCHITECTURE:
#   Eyes (Scanners) → Brain (Intelligence) → Queen Mind → Orca Execution
#   All systems communicate via ThoughtBus pub/sub
#
# ════════════════════════════════════════════════════════════════════════════════

# 👑 COMMAND CENTER - Web UI Dashboard + All Internal Systems
# This loads: ThoughtBus, Mycelium, Scanners, Brain, Queen Mind
[program:command-center]
command=python -u aureon_command_center_ui.py
directory=/app
autostart=true
autorestart=true
priority=1
startsecs=10
startretries=5
stdout_logfile=/dev/fd/1
stderr_logfile=/dev/fd/2
stdout_logfile_maxbytes=0
stderr_logfile_maxbytes=0
environment=PORT="8080",AUREON_STATE_DIR="/app/state",AUREON_ENABLE_AUTONOMOUS_CONTROL="1"

# ⚔️ ORCA KILL CYCLE - Trading Execution Engine
# ONLY starts after Command Center is healthy
# ONLY executes on Queen's commands (via ThoughtBus queen.decision.*)
[program:orca-engine]
command=bash /app/deploy/start_orca.sh
directory=/app
autostart=true
autorestart=true
startretries=10
startsecs=60
priority=10
stdout_logfile=/dev/fd/1
stderr_logfile=/dev/fd/2
stdout_logfile_maxbytes=0
stderr_logfile_maxbytes=0
environment=AUREON_STATE_DIR="/app/state",AUREON_ENABLE_AUTONOMOUS_CONTROL="1"

# 🧠 AUTONOMOUS ENGINE - 9 Parallel Intelligence Loops
# Feeds Queen with additional intelligence signals
[program:autonomous-engine]
command=python -u aureon_autonomous_worker.py
directory=/app
autostart=true
autorestart=true
startretries=5
startsecs=30
priority=20
stdout_logfile=/dev/fd/1
stderr_logfile=/dev/fd/2
stdout_logfile_maxbytes=0
stderr_logfile_maxbytes=0
environment=AUREON_STATE_DIR="/app/state",AUREON_ENABLE_AUTONOMOUS_CONTROL="1"

